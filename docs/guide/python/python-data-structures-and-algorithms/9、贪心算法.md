# 第九章：贪心算法

贪心算法在每一步选择中都采取当前状态下最优的选择，希望导致全局最优解。

---

## 9.1 贪心算法基础

**特点：**
- 局部最优选择
- 不回溯
- 不一定得到全局最优解

---

## 9.2 经典问题

### 活动选择问题

```python
def activity_selection(start, finish):
    """活动选择"""
    n = len(start)
    activities = sorted(zip(finish, start))
    
    selected = [activities[0]]
    last_finish = activities[0][0]
    
    for i in range(1, n):
        if activities[i][1] >= last_finish:
            selected.append(activities[i])
            last_finish = activities[i][0]
    
    return selected

# 测试
start = [1, 3, 0, 5, 8, 5]
finish = [2, 4, 6, 7, 9, 9]
print("选择的活动:", activity_selection(start, finish))
```

### 硬币找零

```python
def coin_change(coins, amount):
    """最少硬币数"""
    coins.sort(reverse=True)
    count = 0
    
    for coin in coins:
        while amount >= coin:
            amount -= coin
            count += 1
    
    return count if amount == 0 else -1

# 测试
print("最少硬币:", coin_change([1, 5, 10, 25], 63))  # 6
```

---

## 本章小结

✅ **贪心策略**：局部最优
✅ **应用场景**：活动选择、找零

> 第十章将学习回溯算法！
